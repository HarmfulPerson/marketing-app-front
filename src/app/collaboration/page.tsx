"use client";

import isAuth from "@/components/isAuth";
import { useState } from "react";

function CollaborationPage() {
    return (
        <div className="px-7 py-4 shadow bg-white rounded-md flex flex-col gap-2">
            ss
        </div>
    );
}

export default isAuth(CollaborationPage);
